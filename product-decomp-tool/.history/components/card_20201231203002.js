const { ReactComponent } = require("*.svg");

import React from 'react';

class Card extends ReactComponent {
    render() {
        return ( <
            div >
            <
            p > < /p> < /
            div >
        );
    }
}